# PnP TSVD Simulation (OpenCV Python)

## Setup
python -m venv .venv
# Windows: .venv\Scripts\activate
source .venv/bin/activate

pip install -r requirements.txt

## Run
python scripts/run_sweep.py

## Output
CSV results go to outputs/*.csv
